{% extends 'base.html.twig' %}

{% block title %}Lien{% endblock %}

{% block body %}
<div class="container mt-5 mb-5">
    <h1>Lien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lien.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ lien.titre }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ lien.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_lien_index') }}">back to list</a>

    <a href="{{ path('admin_lien_edit', {'id': lien.id}) }}">edit</a>

    {{ include('admin/lien/_delete_form.html.twig') }}
    </div>
{% endblock %}
